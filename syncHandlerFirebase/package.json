{
  "name": "smarthome-provider-placeholder",
  "version": "0.0.1",
  "description": "Placeholder backend for the Local Home SDK sample.",
  "private": true,
  "engines": {
    "node": "8"
  },
  "main": "index.js",
  "dependencies": {
    "actions-on-google": "^2.12.0",
    "firebase-admin": "^8.4.0",
    "firebase-functions": "^3.2.0",
    "ts-polyfill": "^3.5.3"
  },
  "devDependencies": {
    "firebase-tools": "^7.3.0",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "initHub": "firebase functions:config:set hub1.leds=16 hub1.channel=1,2,3",
    "build": "tsc",
    "lint": "tslint --project .",
    "compile": "tsc --noEmit",
    "deploy": "firebase deploy --only functions",
    "firebase": "firebase"
  },
  "author": "Google",
  "license": "Apache-2.0"
}
